# FastAPI + MySQL + OCR 后端依赖（云托管版）

# Web框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18

# 数据库
sqlalchemy[asyncio]==2.0.36
aiomysql==0.2.0
pymysql==1.1.1
alembic==1.14.1
aiosqlite==0.20.0

# 认证
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
pydantic[email-validator]==2.10.4
pydantic-settings==2.7.0

# 图片处理
Pillow==11.1.0

# OCR 引擎
# Tesseract：默认引擎，云端无 AVX2 依赖
pytesseract==0.3.13
# Pix2Text 在线 API 调用走 httpx（已在 Web 框架依赖）
# 本地 Pix2Text（可选，仅本地开发用）: pip install pix2text

# Word导出
python-docx==1.1.2
latex2mathml>=0.1.0

# OSS存储
cos-python-sdk-v5==1.9.35

# 工具
httpx==0.28.1
aiofiles==24.1.0
